# NIS Protocol v3.2 - Minimal Requirements for PyPI Publishing
# These are the absolute minimum dependencies needed for the package to build and install

# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Essential Dependencies
numpy>=1.24.0
aiohttp>=3.8.0
redis>=4.5.0

# Build Tools (for PyPI publishing)
setuptools>=65.0.0
wheel>=0.40.0
build>=0.10.0
