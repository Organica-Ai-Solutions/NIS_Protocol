# Core FastAPI and web framework
fastapi>=0.110.0
uvicorn>=0.20.0
starlette>=0.30.0
pydantic>=2.0.0

# Basic utilities
python-dotenv>=1.0.0
requests>=2.30.0
aiohttp>=3.8.0
aiofiles>=23.0.0

# Database and caching
redis>=4.5.0
aiokafka>=0.8.0
sqlalchemy>=2.0.0

# Math and scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Machine Learning - FIXED VERSIONS for compatibility
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# TensorFlow/Keras - EXACT VERSIONS to avoid conflicts
tensorflow==2.15.1
tf-keras==2.15.1
keras==2.15.0

# Transformers - PINNED VERSIONS
transformers==4.35.2
sentence-transformers==2.2.2
tokenizers>=0.15.0

# NVIDIA packages
nvidia-ml-py3>=7.352.0

# NeMo Framework (optional - will install if available)
# nemo_toolkit[all]>=2.4.0

# LangChain ecosystem
langchain>=0.1.0
langgraph>=0.1.0
langsmith>=0.1.0

# Additional ML packages
scikit-learn>=1.3.0
datasets>=2.10.0
accelerate>=0.20.0

# Utilities
click>=8.0.0
python-dateutil>=2.8.0
pytz>=2023.0
colorlog>=6.0.0
tqdm>=4.65.0

# Optional packages for enhanced functionality
# Install only if needed to avoid conflicts
# optimum>=1.12.0
# peft>=0.4.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
