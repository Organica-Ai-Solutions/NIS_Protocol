# 🔑 NIS Protocol v3 - LLM Provider API Keys (REQUIRED)
# =================================================================
# Get your API keys from the respective provider websites:
# • OpenAI: https://platform.openai.com/api-keys
# • Anthropic: https://console.anthropic.com/
# • DeepSeek: https://platform.deepseek.com/
# • Google: https://makersuite.google.com/app/apikey

# At least ONE of these is required for the system to work:
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional providers (enhance capabilities when configured):
DEEPSEEK_API_KEY=your_deepseek_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# 🐳 Infrastructure Configuration (Docker defaults - usually no changes needed)
# =================================================================
COMPOSE_PROJECT_NAME=nis-protocol-v3
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# 🚀 Application Configuration
# =================================================================
NIS_ENV=production
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000
DASHBOARD_PORT=5000

# 🧠 AI/ML Configuration
# =================================================================
# Model preferences (optional - defaults will be used if not specified)
DEFAULT_LLM_PROVIDER=openai
DEFAULT_MODEL=gpt-4
FALLBACK_MODEL=gpt-3.5-turbo
MAX_TOKENS=4000
TEMPERATURE=0.7

# 🔧 well-engineered Configuration (optional)
# =================================================================
# Enable debug mode for development
DEBUG=false

# Maximum number of concurrent AI operations
MAX_CONCURRENT_OPERATIONS=10

# Cache TTL in seconds
CACHE_TTL=3600

# Request timeout in seconds
REQUEST_TIMEOUT=300

# 📊 Monitoring (implemented in src/monitoring/) (see src/Monitoring (implemented in src/monitoring/)/) Configuration (optional)
# =================================================================
ENABLE_METRICS=true
ENABLE_TRACING=false
GRAFANA_ADMIN_PASSWORD=nis_admin_2025

# 🔐 Security Configuration (optional)
# =================================================================
# JWT secret for authentication (auto-generated if not provided)
JWT_SECRET=your_jwt_secret_here_change_in_production

# API rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=100

# ⚠️  IMPORTANT SETUP INSTRUCTIONS:
# =================================================================
# 1. Copy this file to .env: cp environment-template.txt .env
# 2. Replace 'your_*_api_key_here' with your actual API keys
# 3. At minimum, configure either OPENAI_API_KEY or ANTHROPIC_API_KEY
# 4. Keep the .env file secure and never commit it to version control
# 5. After updating API keys, restart the system: ./stop.sh && ./start.sh
#
# 🚀 Ready to start? Run: ./start.sh 