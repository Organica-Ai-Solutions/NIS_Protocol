# Security constraints for NIS Protocol v3.2 - Security Updates
# Exclude vulnerable packages and fix dependency conflicts

# EXCLUDE: keras due to CVE-2024-55459 (file download vulnerability)
# Use tf-keras instead for Keras functionality
keras<0.0.0

# Fix dependency version conflicts
packaging>=23.2,<26.0  # Fix langchain-core conflict
email-validator>=2.0,<3.0  # Fix fastapi-mail conflict
websockets>=10.2,<16.0  # Fix alpaca-py conflict
google-ai-generativelanguage>=0.6.16,<0.7.0  # Fix langchain-google-genai conflict
