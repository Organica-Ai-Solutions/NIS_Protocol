# =============================================================================
# NIS Protocol - AWS ECS Environment Variables
# =============================================================================
# Clean .env file for ECS Task Definitions
# Created: Dec 11, 2025
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 LLM API KEYS (REQUIRED - at least one)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# -----------------------------------------------------------------------------
# ☁️ GOOGLE CLOUD (for Gemini/Vertex AI)
# -----------------------------------------------------------------------------
GCP_PROJECT_ID=organicaaisolutions
GOOGLE_APPLICATION_CREDENTIALS=/app/google-credentials.json
GOOGLE_SERVICE_ACCOUNT_KEY=/app/service-account-key.json

# -----------------------------------------------------------------------------
# 🟢 NVIDIA NIM (for GPU instances - G2)
# -----------------------------------------------------------------------------
NGC_API_KEY=your_ngc_api_key_here
NIM_BASE_URL=https://api.nvcf.nvidia.com/v2/nvcf
NVIDIA_NIM_ENABLED=false

# -----------------------------------------------------------------------------
# 🗄️ AWS INFRASTRUCTURE - UPDATE THESE FOR ECS
# -----------------------------------------------------------------------------

# Redis (AWS ElastiCache endpoint)
# Format: your-cluster.xxxxx.region.cache.amazonaws.com
REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
REDIS_PORT=6379
REDIS_DB=0

# Kafka (AWS MSK bootstrap servers)
# Format: b-1.your-cluster.xxxxx.region.kafka.amazonaws.com:9092,b-2...
KAFKA_BOOTSTRAP_SERVERS=b-1.your-msk-cluster.kafka.amazonaws.com:9092,b-2.your-msk-cluster.kafka.amazonaws.com:9092

# PostgreSQL (AWS RDS - if using)
# DATABASE_URL=postgresql://user:password@your-rds-endpoint.region.rds.amazonaws.com:5432/nis_protocol_v3

# -----------------------------------------------------------------------------
# 🎯 APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
NIS_ENV=production
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000

# Application Behavior
ENABLE_VOICE=true
ENABLE_ANALYTICS=true
ENABLE_CACHING=true

# -----------------------------------------------------------------------------
# 🔐 SECURITY
# -----------------------------------------------------------------------------
JWT_SECRET_KEY=your-production-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION=86400

CORS_ORIGINS=*
CORS_ALLOW_CREDENTIALS=true

# -----------------------------------------------------------------------------
# 🚀 PERFORMANCE TUNING
# -----------------------------------------------------------------------------
WORKERS=4
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=50
MAX_REQUEST_SIZE=10485760
REQUEST_TIMEOUT=300

# -----------------------------------------------------------------------------
# 🔬 AGENT CONFIGURATION
# -----------------------------------------------------------------------------
AGENT_MAX_CONCURRENT=10
AGENT_TIMEOUT=120
ENABLE_PHYSICS_VALIDATION=true
PHYSICS_AUTO_CORRECTION=true
ENABLE_WEB_SEARCH=true
MAX_SEARCH_RESULTS=10

# -----------------------------------------------------------------------------
# 💾 MEMORY & CACHING
# -----------------------------------------------------------------------------
MEMORY_MAX_CONTEXT_LENGTH=4096
MEMORY_EMBEDDING_DIM=768
ENABLE_CONVERSATION_MEMORY=true
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# -----------------------------------------------------------------------------
# 🗄️ VECTOR DATABASE (Optional but recommended)
# -----------------------------------------------------------------------------
VECTOR_STORE_BACKEND=auto
PINECONE_API_KEY=your-pinecone-key-here
PINECONE_ENVIRONMENT=us-west1-gcp
PINECONE_INDEX_NAME=nis-protocol-memory

# -----------------------------------------------------------------------------
# 🎙️ VOICE (Optional)
# -----------------------------------------------------------------------------
ELEVENLABS_API_KEY=your-elevenlabs-key-here
WHISPER_MODEL=base
BARK_VOICE=v2/en_speaker_6

# =============================================================================
# NOTES FOR MANDEEP:
# =============================================================================
# 
# 1. REDIS (ElastiCache):
#    - Set REDIS_HOST to your ElastiCache primary endpoint
#    - Keep REDIS_PORT=6379 (default)
#
# 2. KAFKA (MSK):
#    - Set KAFKA_BOOTSTRAP_SERVERS to your MSK bootstrap servers
#    - Format: comma-separated list of broker endpoints
#    - Example: b-1.cluster.xxx.kafka.amazonaws.com:9092,b-2.cluster.xxx.kafka.amazonaws.com:9092
#    - NO ZOOKEEPER NEEDED - MSK handles this internally
#
# 3. The code uses KAFKA_BOOTSTRAP_SERVERS in:
#    - src/agents/communication/ (Kafka producers/consumers)
#    - Backend service initialization
#
# 4. For C2 (CPU) instances: Set NVIDIA_NIM_ENABLED=false
#    For G2 (GPU) instances: Set NVIDIA_NIM_ENABLED=true
#
# =============================================================================
