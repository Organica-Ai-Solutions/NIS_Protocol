# =============================================================================
# NIS Protocol - AWS ECS MINIMAL Environment Variables
# =============================================================================
# Only REQUIRED variables - no optional features
# For Cloudelligent Team (Mandeep/Momina)
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 LLM API KEYS (at least ONE required)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# 🗄️ AWS INFRASTRUCTURE (REQUIRED)
# -----------------------------------------------------------------------------
# Redis (ElastiCache)
REDIS_HOST=
REDIS_PORT=6379

# Kafka (MSK) - comma-separated bootstrap servers
KAFKA_BOOTSTRAP_SERVERS=

# -----------------------------------------------------------------------------
# 🎯 APPLICATION (REQUIRED)
# -----------------------------------------------------------------------------
NIS_ENV=production
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000

# -----------------------------------------------------------------------------
# 🎮 GPU CONFIGURATION (Tesla T4 on G2 instances)
# -----------------------------------------------------------------------------
# For CPU instances (C2): NVIDIA_NIM_ENABLED=false
# For GPU instances (G2 with Tesla): NVIDIA_NIM_ENABLED=true
NVIDIA_NIM_ENABLED=true
NGC_API_KEY=

# -----------------------------------------------------------------------------
# 🔐 SECURITY (REQUIRED)
# -----------------------------------------------------------------------------
JWT_SECRET_KEY=

# =============================================================================
# TESLA GPU DRIVER NOTES:
# =============================================================================
# AWS G2 instances come with Tesla GPUs
# ECS Optimized GPU AMI includes NVIDIA drivers pre-installed
# No manual driver installation needed - AWS handles this
# 
# Task Definition GPU requirements:
#   "resourceRequirements": [
#     {
#       "type": "GPU",
#       "value": "1"
#     }
#   ]
# =============================================================================
